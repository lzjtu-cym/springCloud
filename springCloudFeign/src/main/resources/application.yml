server:
  port: 8082
spring:
  application:
    name: service-feign

# eureka配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/

# 启动feign中断路器
feign:
  hystrix:
    enabled: true